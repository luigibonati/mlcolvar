name: test
channels:

  - pytorch
  - conda-forge

  - defaults
dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Package dependencies
  - numpy
  - pandas
  - pytorch
  - pydantic<2
  - lightning
  - matplotlib
  - scikit-learn
  - scipy
  - pytorch-scatter
   
    # Pip-only installs
  - pip:
    - KDEpy
    - nbmake

