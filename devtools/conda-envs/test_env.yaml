name: test
channels:

  - conda-forge
  - pytorch

  - defaults
dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Package dependencies
  - numpy
  - pandas
  - pytorch
  - lightning
  - matplotlib
  - scikit-learn
  - scipy<1.11
   
    # Pip-only installs
  - pip:
    - KDEpy
    - nbmake

