name: test
channels:

  - pytorch
  - conda-forge
  - pyg

  - defaults
dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Package dependencies
  - numpy<2
  - pandas
  - pytorch
  - pydantic<2
  - lightning
  - matplotlib
  - scikit-learn
  - scipy
  - torch-scatter
   
    # Pip-only installs
  - pip:
    - KDEpy
    - nbmake

