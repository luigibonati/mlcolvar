name: test
channels:

  - conda-forge
  - pytorch

  - defaults
dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Package dependencies
  - numpy
  - pandas
  - pytorch
  - pytorch-lightning
  - matplotlib
  - scikit-learn

    # Pip-only installs
  - pip:
    - KDEpy

