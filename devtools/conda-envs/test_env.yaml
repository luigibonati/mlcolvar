name: test
channels:

  - conda-forge
  - pytorch

  - defaults
dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Package dependencies
  - numpy
  - pandas
  - pytorch<2.3
  - pydantic<2
  - lightning
  - matplotlib
  - scikit-learn
  - scipy
   
    # Pip-only installs
  - pip:
    - KDEpy
    - nbmake

