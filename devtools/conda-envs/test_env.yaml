name: test
channels:

  - pytorch
  - conda-forge
  - pyg

  - defaults
dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Package dependencies
  - numpy
  - pandas
  - pytorch
  - pydantic<2
  - lightning
  - matplotlib
  - scikit-learn<1.8
  - scipy
  - pyg

    # Pip-only installs
  - pip:
    - KDEpy
    - nbmake
    - mdtraj
    - matscipy


