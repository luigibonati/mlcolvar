name: docs
channels:

  - conda-forge
  - pytorch

  - defaults

dependencies:

  # Base depends
  - python
  - pip

  # Core dependencies
  - numpy
  - pandas
  - pytorch
  - pytorch-lightning

  # utils
  - matplotlib
  - nbsphinx
  - ipython

  # Pip-only installs
  - pip:
    - sphinx-copybutton
    - furo
    - KDEpy

