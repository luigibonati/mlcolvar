name: docs
channels:

  - conda-forge
  - pytorch

  - defaults

dependencies:

  # Base depends
  - python
  - pip

  # Package dependencies
  - numpy
  - pandas
  - pytorch
  - matplotlib
  - nbsphinx
  - ipython
  - scikit-learn

  # Pip-only installs
  - pip:
    - sphinx-copybutton
    - furo
    - KDEpy

